# PostgreSQL Configuration for Xpress Ops Tower
# Use this configuration when running with PostgreSQL

# Database Configuration
DATABASE_TYPE=postgresql
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=xpress_ops_tower
DATABASE_USER=ops_admin
DATABASE_PASSWORD=ops_secure_2024

# Connection Pool Configuration
DB_POOL_MIN=5
DB_POOL_MAX=50
DB_ACQUIRE_TIMEOUT=30000
DB_IDLE_TIMEOUT=300000
DB_REAP_INTERVAL=1000

# Performance Monitoring
SLOW_QUERY_THRESHOLD=2000

# Environment
NODE_ENV=development

# Security
JWT_SECRET=development-jwt-secret-change-in-production
API_KEY=dev-api-key-12345

# Application
PORT=4000